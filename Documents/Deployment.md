## 📦 **Deployment**

To deploy the application, follow these steps:

1. Build the project:

```bash
dotnet build
```

2. Publish the project:

```bash
dotnet publish -c Release -o out
```

3. Run the published project:

